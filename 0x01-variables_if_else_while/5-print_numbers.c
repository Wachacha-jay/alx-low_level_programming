#include <stdio.h>
/**
 * main - entry point
 *
 * print single digit number of base 10
 *
 * Return: always return 0
 */
int main(void)
{
int a = 0;
for (; a < 10 ; a++)
printf("%d", a);
 putchar('\n');
return (0);
}
