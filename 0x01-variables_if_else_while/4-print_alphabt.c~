#include <stdio.h>
/**
 * main - entry point
 *
 * print alphabets in lower followed by newline
 *
 * Return: always return 0
 */
int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
{
if (a == 'q' || a == a == 'e')
{
continue;
}
putchar(a);
}
putchar('\n');
return (0);
}
